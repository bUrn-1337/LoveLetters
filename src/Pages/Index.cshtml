@page
@model IndexModel
@{
    ViewData["Title"] = "Love Letters ";
}

<div class="hero-section">
    <div class="floating-hearts">
        <span class="heart">💕</span>
        <span class="heart">💖</span>
        <span class="heart">💗</span>
        <span class="heart">💘</span>
        <span class="heart">💝</span>
        <span class="heart">❤️</span>
        <span class="heart">💕</span>
        <span class="heart">💖</span>
    </div>
    <h1 class="main-title">Love Letters</h1>
    <p class="subtitle">Create magical love letters with our enchanted template engine</p>
</div>

<div class="container">
    <div class="card love-card">
        <div class="card-header">
            <span class="heart-icon">💌</span>
            <h2>Compose Your Love Letter</h2>
        </div>
        <div class="card-body">
            <form method="post">
                <div class="form-group">
                    <label for="recipientName">💝 Recipient's Name</label>
                    <input type="text" id="recipientName" name="RecipientName" 
                           class="form-control" placeholder="Enter your beloved's name..."
                           value="@Model.RecipientName" />
                </div>
                
                <div class="form-group">
                    <label for="template">✨ Magic Template</label>
                    <textarea id="template" name="Template" class="form-control template-input" 
                              rows="6" placeholder="Write your love letter template here...">@Model.Template</textarea>
                </div>
                
                <button type="submit" class="btn-love">
                    <span class="btn-text">💖 Generate Love Letter 💖</span>
                    <span class="btn-sparkle">✨</span>
                </button>
            </form>
        </div>
    </div>

    @if (!string.IsNullOrEmpty(Model.RenderedLetter))
    {
        <div class="card result-card">
            <div class="card-header">
                <span class="heart-icon">💝</span>
                <h2>Your Enchanted Letter</h2>
            </div>
            <div class="card-body">
                <div class="letter-paper">
                    <div class="letter-content">
                        @Html.Raw(Model.RenderedLetter)
                    </div>
                    <div class="letter-seal">💋</div>
                </div>
            </div>
        </div>
    }

    @if (!string.IsNullOrEmpty(Model.ErrorMessage))
    {
        <div class="card error-card">
            <div class="card-body">
                <p class="error-text">💔 @Model.ErrorMessage</p>
            </div>
        </div>
    }
</div>

<div class="footer">
    <p>Made with 💖 for Valentine's Day 2026</p>
    <p class="footer-hint">Sometimes the most powerful magic is hidden in plain sight...</p>
</div>
